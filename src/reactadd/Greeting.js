import React from 'react';
import Proptypes from 'prop-types';
class Greeting extends React.Component {
    render() {
        return(
            Greeting.proptoTypes ={
                name:Proptypes.string,
                id:Proptypes.number.isRequired,
            }
            Greeting.defaultProps = {
                name: "rock"
        
            };
            export default 

            }

        )
    }
    }
}
